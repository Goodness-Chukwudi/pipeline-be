
import { API_PATH } from "../App";


class AppRoutes {

    private app;
    constructor(app:any) {
        this.app = app;
    }

    initRoutes() {
        // this.app.use(API_PATH + "/app", );

    }
}

export default AppRoutes;